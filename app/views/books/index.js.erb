books = "<%= j(render @books) %>"
$("#view-more-books-container").before("<%= j(render 'books') %>")

<% if @books.last_page? %>
  $("#view-more-books").hide();
<% else %>
  $("#view-more-books").attr("href", "<%= catalog_path(page: (@books.current_page + 1)) %>");
<% end %>