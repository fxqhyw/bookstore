%main.container
  %h1.general-title-margin= t('page.book.index.catalog')
  .hidden-xs.clearfix
    .dropdowns.pull-right
      .dropdown.width-240
        %a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
          = filter_books_title
          %i.fa.fa-angle-down.dropdown-icon.pull-right
        = render 'filter'
    = render 'category'
  = render 'mobile_sort'
  .mb-120#books-container
    = render 'books'
  .text-center#view-more-books-container
    = link_to t('button.view_more'),catalog_path(page: @books.current_page + 1, category: @category_id, filter: @filter), id: 'view-more-books', class: 'btn btn-primary', remote: true
